groups:
  - name: test_alerts
    interval: 30s
    rules:
      - alert: TestAlert
        expr: vector(1) == 1
        for: 10s
        labels:
          severity: warning
        annotations:
          summary: "Test alert - This is a test notification"
          description: "This is a test alert to verify email notifications are working. You can safely ignore this."

