# ===========================================
# Site Configuration
# ===========================================
# Set which site to monitor (one site at a time)
# Available sites: MOH, ONCO
# To switch sites, change this value and run:
#   docker-compose --env-file site.env up -d --force-recreate prometheus
ACTIVE_SITE=MOH

# ===========================================
# S3/AWS Configuration (optional - for long-term storage)
# ===========================================
# Leave empty if not using S3 storage
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET=observability
S3_ENDPOINT=s3.amazonaws.com

